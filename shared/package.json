{
  "name": "@financas-pessoais/shared",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "postbuild": "node -e \"require('./dist/index.js')\"",
    "lint": "eslint src --ext .ts"
  }
}
