import { ReservasView } from '@/components/reservas/ReservasView';

export const metadata = {
  title: 'Reservas — Finanças Pessoais',
};

export default function ReservasPage() {
  return <ReservasView />;
}
