import { RelatoriosView } from '@/components/relatorios/RelatoriosView';

export const metadata = {
  title: 'Relatórios — Finanças Pessoais',
};

export default function RelatoriosPage() {
  return <RelatoriosView />;
}
